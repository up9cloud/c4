[package]
name = "example-01-advanced"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
c4-config = { path = "../..", features = ["yaml"] }
serde = { version = "1", features = ["derive"] }
