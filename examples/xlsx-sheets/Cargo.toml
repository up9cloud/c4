[package]
name = "xlsx-sheets"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
c4-config = { path = "../..", features = ["excel"] }
serde = { version = "1", features = ["derive"] }
